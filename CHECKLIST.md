# AKDev CHECKLIST

このチェックリストは、`ROADMAP.md` を実装作業に落とした進捗管理表です。

`ROADMAP.md` は説明用、こちらは Claude Code に作業させながら `- [ ]` を `- [x]` に変えていくためのリストです。

---

# 0. 進捗ルール

* [ ] 作業完了時は、該当項目を `- [x]` に変更する
* [ ] 途中で仕様変更した場合は、このファイルも更新する
* [ ] 完了条件が曖昧な項目は、先に完了条件を追記する
* [ ] 1回の実装で複数 Phase を進めすぎない
* [ ] 各 Phase の最後に手動テスト結果をメモする

---

# 1. プロジェクト基盤

## 1.1 リポジトリ初期化

* [x] Git リポジトリを作成する
* [x] `main` ブランチを作成する
* [x] `dev` ブランチを作成する
* [x] `.gitignore` を作成する
* [x] Python 用の除外設定を入れる
* [x] ビルド生成物用の除外設定を入れる
* [x] エディタ一時ファイルの除外設定を入れる

## 1.2 基本ファイル

* [x] `README.md` を作成する
* [x] `ROADMAP.md` を配置する
* [x] `CHECKLIST.md` を配置する
* [x] `LICENSE` を配置する
* [x] `requirements.txt` または `pyproject.toml` を作成する
* [x] 起動用 `main.py` を作成する

## 1.3 ディレクトリ構成

* [x] `core/` を作成する
* [x] `ui/` を作成する
* [x] `asm/` を作成する
* [x] `parts/` を作成する
* [x] `spec/` を作成する
* [x] `sample/` を作成する
* [x] `docs/` を作成する
* [x] `build/` を作成する
* [x] `asset/` を作成する

## 1.4 初期仕様書

* [x] `spec/cpu.md` を作成する
* [x] `spec/gpu.md` を作成する
* [x] `spec/bus.md` を作成する
* [x] `spec/mem.md` を作成する
* [x] `spec/rom.md` を作成する
* [x] `spec/part.md` を作成する
* [x] `spec/project.md` を作成する

---

# 2. アプリ起動と GUI 骨組み

## 2.1 アプリ起動

* [x] Python でアプリを起動できる
* [x] 必要ライブラリをインストールできる
* [x] PySide6 を使うか決める
* [x] メインウィンドウを表示できる
* [x] ウィンドウタイトルを `AKDev` にする
* [x] 初期ウィンドウサイズを設定する
* [x] 終了時に正常終了する

## 2.2 メインレイアウト

* [ ] メニューバーを表示する
* [ ] ツールバーを表示する
* [ ] 左側に Parts Library パネルを置く
* [ ] 中央に System Canvas を置く
* [ ] 右側に Properties パネルを置く
* [ ] 下側に Log / Console パネルを置く
* [ ] 中央または下部に Tab Editor 領域を置く
* [ ] レイアウトをリサイズ可能にする

## 2.3 メニュー項目

* [ ] `File > New Project` を追加する
* [ ] `File > Open Project` を追加する
* [ ] `File > Save Project` を追加する
* [ ] `File > Exit` を追加する
* [ ] `Build > Build` を追加する
* [ ] `Run > Run` を追加する
* [ ] `Run > Pause` を追加する
* [ ] `Run > Step` を追加する
* [ ] `Run > Reset` を追加する
* [ ] `View > Bus Trace` を追加する
* [ ] `View > Memory` を追加する
* [ ] `View > Registers` を追加する

## 2.4 ツールバー

* [ ] New ボタンを追加する
* [ ] Open ボタンを追加する
* [ ] Save ボタンを追加する
* [ ] Build ボタンを追加する
* [ ] Run ボタンを追加する
* [ ] Pause ボタンを追加する
* [ ] Step ボタンを追加する
* [ ] Reset ボタンを追加する

---

# 3. プロジェクト保存形式

## 3.1 プロジェクト作成

* [ ] 新規プロジェクト用ディレクトリを作成できる
* [ ] `project.json` を生成できる
* [ ] `system.json` を生成できる
* [ ] `src/` を生成できる
* [ ] `asset/` を生成できる
* [ ] `build/` を生成できる
* [ ] `parts/custom/` を生成できる

## 3.2 `project.json`

* [ ] プロジェクト名を保存する
* [ ] 作成日時を保存する
* [ ] AKDev のバージョンを保存する
* [ ] 使用プリセット名を保存する
* [ ] 最後に開いていたファイルを保存する

## 3.3 `system.json`

* [ ] チップ一覧を保存する
* [ ] パーツ一覧を保存する
* [ ] 接続一覧を保存する
* [ ] パーツ座標を保存する
* [ ] パーツ設定を保存する
* [ ] メモリマップを保存する

## 3.4 保存/読み込み

* [ ] プロジェクトを保存できる
* [ ] プロジェクトを開ける
* [ ] 保存後に再起動しても配置が復元される
* [ ] 不正な JSON を読んだ時にエラーを出す
* [ ] バージョン違いの警告を出せる

---

# 4. パーツライブラリ

## 4.1 パーツ定義形式

* [ ] `part.json` の必須項目を決める
* [ ] `id` を必須にする
* [ ] `name` を必須にする
* [ ] `category` を必須にする
* [ ] `ports` を必須にする
* [ ] `resources` を任意項目にする
* [ ] `editable` を任意項目にする
* [ ] `sim` を任意項目にする

## 4.2 パーツ読み込み

* [ ] `parts/` 以下を再帰的に走査する
* [ ] `part.json` を検出する
* [ ] JSON を読み込む
* [ ] 必須項目を検証する
* [ ] 無効なパーツをスキップする
* [ ] エラー内容を Log に出す
* [ ] 有効なパーツを Parts Library に表示する

## 4.3 初期パーツ作成

* [ ] `fpga.ecp5_85f` を作る
* [ ] `fpga.generic` を作る
* [ ] `cpu.ak32` を作る
* [ ] `mem.ram` を作る
* [ ] `mem.vram` を作る
* [ ] `io.uart` を作る
* [ ] `io.timer` を作る
* [ ] `io.gpio` を作る
* [ ] `video.regs` を作る
* [ ] `video.out` を作る
* [ ] `bus.bridge` を作る

## 4.4 パーツカテゴリ表示

* [ ] FPGA カテゴリを表示する
* [ ] CPU カテゴリを表示する
* [ ] Memory カテゴリを表示する
* [ ] I/O カテゴリを表示する
* [ ] Video カテゴリを表示する
* [ ] Debug カテゴリを表示する
* [ ] Custom カテゴリを表示する

## 4.5 自作パーツ

* [ ] `parts/custom/` を読み込む
* [ ] 自作パーツを Custom カテゴリに表示する
* [ ] 自作パーツのエラーを表示する
* [ ] パーツテンプレートを用意する
* [ ] Memory パーツテンプレートを作る
* [ ] Bus Device パーツテンプレートを作る
* [ ] Video Device パーツテンプレートを作る
* [ ] FPGA パーツテンプレートを作る

---

# 5. System Canvas

## 5.1 パーツ配置

* [ ] Parts Library からパーツを追加できる
* [ ] ダブルクリックでパーツを追加できる
* [ ] ドラッグ&ドロップでパーツを追加できるか検討する
* [ ] パーツをキャンバス上に表示する
* [ ] パーツ名を表示する
* [ ] パーツ ID を表示する
* [ ] パーツ種別ごとに見た目を変える
* [ ] パーツを移動できる
* [ ] パーツを選択できる
* [ ] 複数選択に対応するか決める
* [ ] Delete キーで削除できる

## 5.2 パーツ座標保存

* [ ] パーツの x 座標を保存する
* [ ] パーツの y 座標を保存する
* [ ] プロジェクト読み込み時に位置を復元する
* [ ] 不正な座標を補正する

## 5.3 ポート表示

* [ ] パーツにポートを表示する
* [ ] 入力ポートと出力ポートの見た目を分ける
* [ ] ポート名を表示する
* [ ] ポート型を保持する
* [ ] ポートにマウスオーバーした時に説明を表示する

## 5.4 接続線

* [ ] ポートから接続線を引ける
* [ ] 接続先ポートに接続できる
* [ ] 無効な接続を拒否する
* [ ] 接続線を表示する
* [ ] 接続線を選択できる
* [ ] 接続線を削除できる
* [ ] 接続情報を `system.json` に保存する
* [ ] 接続情報を復元する

## 5.5 Canvas 操作

* [ ] 拡大縮小を検討する
* [ ] スクロールを追加する
* [ ] グリッド表示を追加する
* [ ] グリッドにスナップする
* [ ] パーツ整列機能を検討する

---

# 6. 右クリックメニューとパーツ操作

## 6.1 共通メニュー

* [ ] パーツ右クリックでメニューを出す
* [ ] `プログラムを開く` を追加する
* [ ] `HDLを開く` を追加する
* [ ] `設定` を追加する
* [ ] `信号を見る` を追加する
* [ ] `メモリを見る` を追加する
* [ ] `複製` を追加する
* [ ] `削除` を追加する

## 6.2 メニュー動作

* [ ] `プログラムを開く` でタブを開く
* [ ] `HDLを開く` でタブを開く
* [ ] `設定` で Properties パネルに移動する
* [ ] `メモリを見る` で Memory Viewer を開く
* [ ] `複製` で同種パーツを作る
* [ ] `削除` でパーツを消す

## 6.3 パーツ別メニュー

* [ ] CPU パーツでは Register View を開ける
* [ ] RAM パーツでは Memory Viewer を開ける
* [ ] UART パーツでは UART Console を開ける
* [ ] Video パーツでは Virtual Screen を開ける
* [ ] FPGA パーツでは Resource View を開ける

---

# 7. Properties パネル

## 7.1 基本表示

* [ ] 選択中パーツ名を表示する
* [ ] パーツ ID を表示する
* [ ] パーツ種別を表示する
* [ ] 親チップを表示する
* [ ] パーツ説明を表示する

## 7.2 基本設定

* [ ] 名前を編集できる
* [ ] ベースアドレスを編集できる
* [ ] サイズを編集できる
* [ ] クロックを編集できる
* [ ] バス幅を編集できる
* [ ] 親チップを変更できるか検討する

## 7.3 FPGA 設定

* [ ] LUT 数を表示する
* [ ] BRAM 容量を表示する
* [ ] DSP 数を表示する
* [ ] 使用 LUT の仮値を表示する
* [ ] 使用 BRAM の仮値を表示する
* [ ] 使用率が高い時に警告する

## 7.4 設定保存

* [ ] 編集内容を即時反映する
* [ ] 編集内容を `system.json` に保存する
* [ ] 無効な値を拒否する
* [ ] 16進数入力を扱える
* [ ] `MiB` などの単位入力を扱える

---

# 8. タブエディタ

## 8.1 エディタ基盤

* [ ] タブを作成できる
* [ ] タブを閉じられる
* [ ] タブ名を表示する
* [ ] 未保存マークを表示する
* [ ] テキスト編集できる
* [ ] ファイルへ保存できる
* [ ] ファイルから読み込める

## 8.2 パーツとの連携

* [ ] CPU の `プログラムを開く` で `.asm` を開く
* [ ] FPGA の `HDLを開く` で `.v` または `.sv` を開く
* [ ] GPU の `HDLを開く` で GPU 定義を開く
* [ ] RAM の `メモリを見る` でメモリタブを開く
* [ ] 同じパーツを二重に開かない
* [ ] 保存時にパーツ状態を更新する

## 8.3 補助機能

* [ ] 行番号を表示する
* [ ] 簡易シンタックスハイライトを追加する
* [ ] 現在行を強調表示する
* [ ] エラー行を強調表示する
* [ ] 検索機能を追加する
* [ ] 置換機能を検討する

---

# 9. シミュレータ本体

## 9.1 基本クラス

* [ ] `Part` クラスを作る
* [ ] `Chip` クラスを作る
* [ ] `Bus` クラスを作る
* [ ] `Sim` クラスを作る
* [ ] `Signal` クラスを作るか検討する
* [ ] `Port` クラスを作る

## 9.2 シミュレーション制御

* [ ] Reset を実装する
* [ ] Run を実装する
* [ ] Pause を実装する
* [ ] Step を実装する
* [ ] Tick loop を実装する
* [ ] Cycle count を実装する
* [ ] 実行速度制限を実装するか検討する

## 9.3 パーツ実行

* [ ] 全パーツの `reset()` を呼ぶ
* [ ] 全パーツの `tick()` を呼ぶ
* [ ] CPU パーツを優先して tick するか決める
* [ ] Video パーツの更新周期を決める
* [ ] Timer パーツの周期を決める

## 9.4 ログ

* [ ] 実行ログを出す
* [ ] エラーログを出す
* [ ] デバイスログを出す
* [ ] Bus Trace を出す
* [ ] ログをクリアできる
* [ ] ログをファイルへ保存できる

---

# 10. Bus

## 10.1 アドレスルーティング

* [ ] メモリマップを保持する
* [ ] アドレスから対象パーツを探す
* [ ] `read(addr)` を対象パーツへ転送する
* [ ] `write(addr, value)` を対象パーツへ転送する
* [ ] 未割当アドレスでエラーを出す
* [ ] アドレス重複を検出する

## 10.2 Bus Trace

* [ ] read を記録する
* [ ] write を記録する
* [ ] cycle を記録する
* [ ] address を記録する
* [ ] value を記録する
* [ ] target part を記録する
* [ ] フィルタを追加する

## 10.3 チップ間バス

* [ ] `parallel16` 接続を定義する
* [ ] `parallel32` 接続を定義するか検討する
* [ ] chip0 と chip1 の bridge パーツを作る
* [ ] チップ間 read/write をシミュレートする
* [ ] チップ間通信ログを分けて表示する

---

# 11. AK32 CPU

## 11.1 CPU 状態

* [ ] `r0` を常に 0 にする
* [ ] `r1` 〜 `r15` を実装する
* [ ] `pc` を実装する
* [ ] `flags` を実装する
* [ ] HALT 状態を実装する
* [ ] Reset PC を設定できるようにする

## 11.2 命令実行の流れ

* [ ] Fetch を作る
* [ ] Decode を作る
* [ ] Execute を作る
* [ ] PC 更新を作る
* [ ] 例外時に停止する
* [ ] 不正命令を検出する

## 11.3 算術・論理命令

* [ ] `NOP`
* [ ] `HALT`
* [ ] `LDI`
* [ ] `ADD`
* [ ] `SUB`
* [ ] `AND`
* [ ] `OR`
* [ ] `XOR`
* [ ] `SHL`
* [ ] `SHR`

## 11.4 メモリ命令

* [ ] `LD`
* [ ] `ST`
* [ ] `IN`
* [ ] `OUT`
* [ ] byte / halfword / word アクセスをどう扱うか決める
* [ ] アラインメント違反をどう扱うか決める

## 11.5 分岐命令

* [ ] `JMP`
* [ ] `BEQ`
* [ ] `BNE`
* [ ] `CALL`
* [ ] `RET`
* [ ] 相対分岐と絶対分岐の扱いを決める

## 11.6 CPU 表示

* [ ] Register View を作る
* [ ] PC を表示する
* [ ] Flags を表示する
* [ ] 現在命令を表示する
* [ ] 直前命令を表示する
* [ ] Step ごとに更新する

---

# 12. アセンブラ

## 12.1 基本処理

* [ ] 行を読み込む
* [ ] コメントを除去する
* [ ] 空行を無視する
* [ ] トークン化する
* [ ] 命令名を解析する
* [ ] レジスタ名を解析する
* [ ] 即値を解析する
* [ ] 16進数を解析する
* [ ] ラベルを解析する

## 12.2 ラベル処理

* [ ] 1パス目でラベル位置を集める
* [ ] 2パス目でラベルを解決する
* [ ] 未定義ラベルを検出する
* [ ] 重複ラベルを検出する

## 12.3 命令エンコード

* [ ] `NOP` をエンコードする
* [ ] `HALT` をエンコードする
* [ ] `LDI` をエンコードする
* [ ] `LD` をエンコードする
* [ ] `ST` をエンコードする
* [ ] 算術命令をエンコードする
* [ ] 論理命令をエンコードする
* [ ] 分岐命令をエンコードする
* [ ] `CALL` / `RET` をエンコードする
* [ ] `IN` / `OUT` をエンコードする

## 12.4 エラー表示

* [ ] 不正命令を表示する
* [ ] 不正レジスタを表示する
* [ ] 即値範囲外を表示する
* [ ] 引数不足を表示する
* [ ] 引数過多を表示する
* [ ] エラー行番号を表示する
* [ ] GUI の Error パネルへ出す

## 12.5 GUI 統合

* [ ] Build ボタンからアセンブラを呼ぶ
* [ ] 成功時にバイナリを生成する
* [ ] 成功時に CPU へロードする
* [ ] 失敗時にエラーを表示する
* [ ] Build Log を表示する

---

# 13. RAM / UART / Timer

## 13.1 RAM

* [ ] RAM パーツを実装する
* [ ] サイズを設定できる
* [ ] ベースアドレスを設定できる
* [ ] `read()` を実装する
* [ ] `write()` を実装する
* [ ] 範囲外アクセスを検出する
* [ ] メモリ内容をクリアできる
* [ ] バイナリを読み込める
* [ ] バイナリを書き出せる

## 13.2 Memory Viewer

* [ ] アドレス入力を作る
* [ ] 16進表示を作る
* [ ] ASCII 表示を作る
* [ ] 値を編集できる
* [ ] Watch アドレスを登録できる
* [ ] 変更箇所を強調表示する

## 13.3 UART

* [ ] UART パーツを実装する
* [ ] write で文字を表示する
* [ ] UART Console を作る
* [ ] 改行を処理する
* [ ] 制御文字の扱いを決める
* [ ] status register を仮実装する
* [ ] read を仮実装する

## 13.4 Timer

* [ ] Timer パーツを実装する
* [ ] cycle count を持たせる
* [ ] compare register を持たせる
* [ ] enable flag を持たせる
* [ ] IRQ flag を持たせる
* [ ] Timer View を作る

---

# 14. グラフィック基礎

## 14.1 Graphics Register

* [ ] Graphics Register パーツを作る
* [ ] `GFX_ID` を実装する
* [ ] `STATUS` を実装する
* [ ] `BG_COLOR` を実装する
* [ ] `CMD` を実装する
* [ ] `ARG0` を実装する
* [ ] `ARG1` を実装する
* [ ] `ARG2` を実装する
* [ ] `IRQ_EN` を実装する
* [ ] `IRQ_FLAG` を実装する

## 14.2 Virtual Screen

* [ ] Virtual Screen パネルを作る
* [ ] 解像度を設定できる
* [ ] 背景色を表示できる
* [ ] `BG_COLOR` 書き込みで画面色を変更する
* [ ] 画面更新ログを出す

## 14.3 最初の GPU テスト

* [ ] CPU から `GFX_ID` を読む
* [ ] CPU から `BG_COLOR` に書く
* [ ] Virtual Screen の色が変わる
* [ ] Bus Trace に GPU write が出る
* [ ] Graphics Register View が更新される

---

# 15. VRAM / Framebuffer

## 15.1 VRAM

* [ ] VRAM パーツを作る
* [ ] サイズを設定できる
* [ ] ベースアドレスを設定できる
* [ ] `read()` を実装する
* [ ] `write()` を実装する
* [ ] 範囲外アクセスを検出する

## 15.2 Palette

* [ ] 256色パレットを持たせる
* [ ] パレットを編集できる
* [ ] パレットを保存できる
* [ ] パレットを読み込める

## 15.3 Framebuffer

* [ ] 8-bit indexed colour を実装する
* [ ] VRAM をピクセルとして解釈する
* [ ] Virtual Screen に描画する
* [ ] ピクセル座標を表示する
* [ ] VRAM 更新時に画面へ反映する

## 15.4 VRAM Viewer

* [ ] VRAM の16進表示を作る
* [ ] VRAM の画像表示を作る
* [ ] パレット表示を作る
* [ ] 選択ピクセルの値を表示する

---

# 16. タイル / スプライト

## 16.1 Tile

* [ ] Tile Memory を作る
* [ ] Tile Map を作る
* [ ] 8x8 tile を表示できる
* [ ] Tile ID を使って背景を描画する
* [ ] Palette index を扱う
* [ ] Scroll X を実装する
* [ ] Scroll Y を実装する

## 16.2 Sprite

* [ ] Sprite Table を作る
* [ ] Sprite X 座標を持たせる
* [ ] Sprite Y 座標を持たせる
* [ ] Tile ID を持たせる
* [ ] 表示/非表示フラグを持たせる
* [ ] 水平反転を検討する
* [ ] 垂直反転を検討する
* [ ] 優先順位を後回しにする

## 16.3 VBlank

* [ ] フレームカウントを作る
* [ ] VBlank flag を作る
* [ ] VBlank IRQ を仮実装する
* [ ] Step Frame を追加する

---

# 17. プリセット / プロジェクト管理

## 17.1 新規プロジェクトウィザード

* [ ] プロジェクト名を入力できる
* [ ] 保存先を選べる
* [ ] プリセットを選べる
* [ ] 作成後にプロジェクトを開く

## 17.2 プリセット

* [ ] Minimal CPU プリセットを作る
* [ ] Simple Video プリセットを作る
* [ ] Dual Chip Console プリセットを作る
* [ ] Handheld Prototype プリセットを作る

## 17.3 プロジェクトツリー

* [ ] `src/` を表示する
* [ ] `asset/` を表示する
* [ ] `build/` を表示する
* [ ] `parts/custom/` を表示する
* [ ] ファイルをダブルクリックで開く

---

# 18. アセットツール

## 18.1 画像インポート

* [ ] PNG を読み込める
* [ ] プレビューできる
* [ ] 画像サイズを表示する
* [ ] 色数を表示する

## 18.2 変換

* [ ] パレットを抽出する
* [ ] 8-bit indexed colour に変換する
* [ ] 8x8 tile に分割する
* [ ] Sprite data を生成する
* [ ] Binary asset を出力する

## 18.3 シミュレータ連携

* [ ] 変換結果を VRAM にロードする
* [ ] Tile Viewer で確認する
* [ ] Sprite Viewer で確認する
* [ ] Virtual Screen に表示する

---

# 19. ROM Builder

## 19.1 ROM 形式

* [ ] ROM header を決める
* [ ] entry point を決める
* [ ] code section を決める
* [ ] asset section を決める
* [ ] metadata section を決める

## 19.2 ROM 生成

* [ ] code binary を格納する
* [ ] asset binary を格納する
* [ ] metadata を格納する
* [ ] `game.rom` を出力する
* [ ] ROM サイズを表示する

## 19.3 ROM 読み込み

* [ ] ROM をエミュレータに読み込む
* [ ] header を検証する
* [ ] entry point から実行する
* [ ] 不正 ROM を拒否する

---

# 20. 実機向けエクスポート

## 20.1 仕様ファイル出力

* [ ] メモリマップを出力する
* [ ] レジスタ定義を出力する
* [ ] バス仕様を出力する
* [ ] パーツ一覧を出力する
* [ ] リソース見積もりを出力する

## 20.2 HDL 支援

* [ ] Verilog 雛形を出力する
* [ ] レジスタ定義ヘッダを出力する
* [ ] テストベンチ雛形を出力する
* [ ] Bus slave 雛形を出力する
* [ ] UART モニタ雛形を出力する

## 20.3 実機デバッグ支援

* [ ] UART 転送用 binary を出力する
* [ ] モニタコマンド定義を出力する
* [ ] power monitor register 定義を出力する

---

# 21. 実機接続

## 21.1 UART 接続

* [ ] シリアルポート一覧を取得する
* [ ] ボーレートを選べる
* [ ] 接続できる
* [ ] 切断できる
* [ ] 接続状態を表示する

## 21.2 転送

* [ ] バイナリを送信できる
* [ ] 送信進捗を表示する
* [ ] 送信完了を表示する
* [ ] 送信エラーを表示する

## 21.3 実機ログ

* [ ] UART ログを受信する
* [ ] ログを表示する
* [ ] ログを保存できる
* [ ] 実機からのエラーを強調する

## 21.4 実機監視

* [ ] 電圧を表示する
* [ ] 電流を表示する
* [ ] 温度を表示する
* [ ] エラーフラグを表示する
* [ ] 管理MCUとの通信形式を決める

---

# 22. 最初の実用マイルストーン

## AKDev v0.1 Minimal Interactive Simulator

* [ ] メインウィンドウが開く
* [ ] Parts Library が見える
* [ ] System Canvas が見える
* [ ] `chip0` を配置できる
* [ ] `AK32` を配置できる
* [ ] `RAM` を配置できる
* [ ] `UART` を配置できる
* [ ] パーツ右クリックメニューが出る
* [ ] `プログラムを開く` でタブが開く
* [ ] asm を保存できる
* [ ] asm をビルドできる
* [ ] CPU がプログラムを実行できる
* [ ] UART に `Hi` が出る
* [ ] Register View が更新される
* [ ] Bus Trace に write が出る

---

# 23. 初期バージョンでやらないこと

この節は、初期開発の範囲外を明確にするためのリストです。

* [ ] 完全な Verilog 合成はしない
* [ ] FPGA タイミングシミュレーションはしない
* [ ] C コンパイラは作らない
* [ ] Linux 対応はしない
* [ ] 3D グラフィックはしない
* [ ] HDMI シミュレーションはしない
* [ ] PCB 自動生成はしない
* [ ] 自動配置配線はしない
* [ ] DS 相当のサイクル精度は狙わない
